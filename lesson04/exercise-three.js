//5 string array
const myArray = ["one", "two", "three", "four", "five"];

//loop through the array, log each element
for (let i = 0; i < myArray.length; i++) {
  console.log(`${i + 1},${myArray[i]}`);
}