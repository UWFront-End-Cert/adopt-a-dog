function information(name, breed, adoptionFees) {
    alert(`Name: ${name}\nBreed: ${breed}\nFees: $${adoptionFees}`);
}
let total = 0;
function adopt() {
    total += 123.45
    alert(`Total: $${total.toFixed(2)}`);
}